import type {PresetMeta} from '../types'

const meta: PresetMeta = {
    id: 'github',
    name: 'GitHub',
    tagline: 'Primer 的路子：深色顶栏 + 一排 tab，内容是一张带边框的清单',
    theme: 'github',
    dashboard: false,
}

export default meta
